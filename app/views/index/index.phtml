<?php

echo "<h1>Hi</h1>";

echo $this->tag->linkTo(
    "signup",
    "Sign Up Here!"
);

if($users->count() > 0) {
    foreach($users as $u) {
        echo $u->id . " " . $u->name . " " . $u->email . "<br />";
    }
}